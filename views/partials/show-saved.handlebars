{{#each savedArticles}}
    <div data-id={{this._id}} class="article">
        <div class="article card">
            <div class="col-lg-10 col-md-10 col-sm-10 col-xs-10">
                <div data-saved-headline={{@index}} class="savedHeadline">
                    <h2><a data-saved-link={{@index}} href="{{this.link}}" target="_blank">{{this.title}}</a></h2>
                    <p data-saved-summary={{@index}} class="savedSummary">{{this.summary}}</p>
                </div>
                
                <div class="btn-wrapper">
                  <!-- render a notes button -->
                  <button type="button" data-article-id={{this._id}} data-toggle="modal" data-target="#noteModal" data-saved-index={{@index}} class="notes btn btn-primary">Add/View Notes</button>
                  
                  <!-- render a delete button -->
                  <button type="button" data-article-id={{this._id}} data-delete-index={{@index}} class="remove btn btn-danger">Remove</button>
                </div>  
            </div><!-- end article column -->
            
                

            
        </div><!-- /end card -->
    </div><!-- /end article -->
{{/each}}

<!-- Modal -->
<div id="noteModal" class="modal fade" role="dialog">
  <div class="modal-dialog">

    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Article notes</h4>
      </div>
      <div class="modal-body">
        <span id="modal-notes"></span>
        
            <div id="note-form" class="form-group">
                <label for="noteText">Enter New Note</label>
                <textarea class="form-control" name="bodyinput" id="bodyinput" rows="3"></textarea>
                {{!-- <input name="articleId" id="articleId" type="hidden"/> --}}
                <button type="submit" id="articleId" class="notebtn btn btn-primary" data-dismiss="modal">Submit</button>
            </div>
        
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" id="savenote" data-dismiss="modal">Close</button>
      </div>
    </div>

  </div>
</div>